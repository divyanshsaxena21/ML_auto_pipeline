from setuptools import setup

# Minimal stub: all metadata is defined in pyproject.toml (PEP 621).
# This file exists only for legacy workflows invoking `python setup.py ...`.
# Do not duplicate metadata here to avoid divergence.

if __name__ == "__main__":
    setup()
