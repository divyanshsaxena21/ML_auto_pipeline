from setuptools import setup

# Legacy fallback. Prefer building with:
#   python -m build
# using the metadata defined in pyproject.toml (PEP 621).
# This file remains for older tooling invoking `python setup.py install`.

if __name__ == "__main__":
    setup()
